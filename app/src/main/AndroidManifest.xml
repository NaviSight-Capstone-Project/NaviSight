<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NaviSight">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".auth.ui.login.LoginActivity" />
        <activity android:name=".viu.ui.ViuHomeActivity" />
        <activity android:name=".caregiver.CaregiverHomeActivity" />

        <activity
            android:name=".auth.ui.signup.caregiver.CaregiverSignupActivity"
            android:exported="false"
            android:label="Sign Up"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".auth.ui.signup.viu.ViuSignupActivity"
            android:exported="false"
            android:label="Sign Up"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name="com.yalantis.ucrop.UCropActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />

        <activity
            android:name=".auth.ui.signup.RoleSelectionActivity"
            android:exported="false"
            android:label="Select Role" />

    </application>

</manifest>